version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user
    overwrite: yes
    
hook:

  ApplicationStop:
    - location: kill_scripts.sh
  BeforeInstall:
    - location: remove_before.sh
  ApplicationStart:
    - location: run_process.sh
      timeout: 60
      runas: root
